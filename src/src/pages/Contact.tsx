import React from "react";
import ContactUs from "../components/ContactUs"; // Adjust path if needed
import "../styles/Contact.css"; // Keep this if you need it for layout/styling

const Contact: React.FC = () => {
  
  return <ContactUs />;
};

export default Contact;
